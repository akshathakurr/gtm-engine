{
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "description": "The URL that was scraped."
    },
    "markdown": {
      "type": ["string", "null"],
      "description": "Main page content as markdown, or null if the key is unset or the scrape failed."
    },
    "chars": {
      "type": "integer",
      "description": "Length of the returned markdown (0 when null)."
    }
  },
  "required": ["url", "markdown", "chars"]
}
