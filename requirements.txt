anthropic>=0.40.0
apify-client>=3,<4   # 3.x: .call() returns a Run object (not a dict); pinned so the API contract can't drift back to 1.x
exa-py>=1.0.0
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pytrends>=4.9.0   # used by scrapers/keyword_validator (optional — only needed if you run blog_builder with --validate-keywords)
